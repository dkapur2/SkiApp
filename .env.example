# Express listens on this port locally. Railway supplies PORT in production.
PORT=8000

# Required only for POST /recommend.
ANTHROPIC_API_KEY=

# Optional. Without this key, resort weather still works and ski status is null.
RAPIDAPI_KEY=
RAPIDAPI_SKI_HOST=ski-resort-forecast.p.rapidapi.com
